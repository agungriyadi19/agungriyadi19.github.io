{
  "name": "pwa-2",
  "version": "1.0.0",
  "description": "",
  "main": "push.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "firebase": "firebase init",
    "deploy": "firebase deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "web-push": "^3.4.4"
  }
}
